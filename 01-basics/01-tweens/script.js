// gsap.to(target, { properties })
gsap.to(".tween-box", {
  x: 300,           // Move 300px to the right
  rotation: 360,    // Spin a full circle
  backgroundColor: "#ff0000", // Change color
  duration: 2,      // Animation lasts 2 seconds
  delay: 0.5        // Wait half a second before starting
});